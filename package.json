{
  "private": true,
  "type": "module",
  "scripts": {
    "preBuild": "node scripts/index.js",
    "build": "npm run preBuild && nuxt build",
    "dev": "npm run preBuild && nuxt dev",
    "generate": "npm run preBuild && nuxt generate",
    "preview": "npm run preBuild && nuxt preview",
    "staticbuild": "npm run preBuild && npx nuxi generate"
  },
  "dependencies": {
    "@google/generative-ai": "^0.8.0",
    "@mdi/font": "^7.4.47",
    "@nuxt-themes/typography": "^1.0.1",
    "@nuxt/content": "^2.12.1",
    "@nuxt/image": "^1.7.0",
    "@nuxtjs/robots": "^3.0.0",
    "@nuxtjs/sitemap": "^5.2.0",
    "@tailwindcss/typography": "^0.5.12",
    "@vueuse/core": "^10.9.0",
    "nuxt": "^3.11.2",
    "openai": "^4.38.5"
  },
  "devDependencies": {
    "nuxt-gtag": "^2.0.5",
    "vite-plugin-vuetify": "^2.0.3",
    "vuetify": "^3.5.17"
  }
}
